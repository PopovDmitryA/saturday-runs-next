# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.venv-parkrun/
.conda-parkrun/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
.coverage
htmlcov/

# Environment
.env
.env.local
*.ini
!backend/alembic.ini

# Локальный оверрайд docker-стека конкретного worktree: у каждого свои порты,
# своя БД (dev или прод через туннель). Общего значения не имеет, а попав в
# репозиторий — перетирает настройки соседних worktree.
docker-compose.worktree-override.yml

# Node
node_modules/
frontend/dist/

# IDE
.idea/
.vscode/
*.swp
.DS_Store

# Docker
postgres_data/

# Runtime artifacts / operational backups
backend/celerybeat-schedule
backend/scripts/backups/

# Local / agent (не для публичного репозитория)
CLAUDE.md
docs/claude_handoff_answers.json
docs/mockups/
frontend/public/favicon-options/
frontend/public/platform-logos/options/
archive.local/
vpn.local/
.cursor/
.claude/
PROJECT_HANDOFF.local.md
LOCATION_PAGES_STATUS.local.md
RATINGS_HANDOFF.local.md
S95_API.md
docs/release_posts_style.md
data/parkrun_playwright_state.json
data/grafana/dashboards/
data/*.log
# Локальный режим media-хранилища (на проде картинки живут в S3).
data/media/
data/avatars/

# Local club-stats outputs (not for repo)
.club_members.json
psb_sport_club_stats.csv

# Временный файл заглушки деплоя (кладёт/убирает remote_deploy.sh на проде)
deploy/nginx/maintenance_current.html

# Реальный конфиг xray-клиента (admin-уведомления в Telegram через прокси) —
# содержит приватный VLESS-профиль (UUID, reality public key). Шаблон см. config.example.json.
deploy/tg-proxy/config.json
