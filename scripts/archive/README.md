# Отработавшие серверные скрипты

## `grafana/` — закрытая легаси-статистика

Grafana на `grafana.run5k.run` закрыта 04.09.2026: домен отдаёт прощальную
заглушку, счётчик посещений снят, дашборды перенесены в разделы сайта.
Скрипты экспорта дашбордов, выпуска TLS, закрытия отдельного дашборда, счётчика
и отчёта по нему свою работу сделали.

В `scripts/` намеренно остались двое: `stop_legacy_grafana.sh` — им гасят сам
сервис, и он ещё ждёт sudo на проде, — и `install_grafana_farewell.sh`, который
он зовёт.

## `domains/` — переезд на run5k.run

Разовая операция июня 2026: apex-домен отдали личному кабинету, `app.run5k.run`
увели редиректом. Порядок действий описан в `deploy/DOMAINS.md`.
`deploy_app_run5k.sh` был обёрткой-заглушкой над `deploy_run5k_domains.sh`.

Перенесено 21.09.2026. История сохранена:
`git log --follow scripts/archive/<папка>/<файл>`.
